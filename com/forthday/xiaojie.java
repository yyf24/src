package com.forthday;

public class xiaojie {
    public static void main(String[] args) {
        /*
        *(1)判断存储类型（单列还是键值对）
        *（2）单列collection
        *       允许重复List
        *                   增删多：LinkedList[底层维护一个双向列表]
        *                   查改多：ArrayList(Vector)[object底层数组]
        *        不允许重复：Set
        *                    无序HashSet
        *                    排序TreeSet
        *                    插入和取出顺序一至LinkedHashSet
        * (3)键值对：Map
        *          键无序HashMap(HashTable)
        *          键排序TreeMap
        *          插入和去除顺序一致LinkedHashMap
        *          读取文件properties
         */
    }
}
